<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.captainhampton.ukulelechords">

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UkuleleActivity"
            android:label="@string/category_ukulele" />

        <activity
            android:name=".UkuleleChordActivity"
            android:label="@string/category_chord" />

        <activity
            android:name=".UkuleleSelectedChordActivity"
            android:label="@string/category_selected_chord" />

        <activity
            android:name=".TunerActivity"
            android:label="@string/category_ukulele_tuner" />

        <activity
            android:name=".AboutActivity"
            android:label="@string/category_about" />

        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            />

    </application>

</manifest>